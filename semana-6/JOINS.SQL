SELECT CLIENTES.NOME,PRODUTOS.NOME  FROM PEDIDOS AS P
INNER JOIN PRODUTOS_X_PEDIDO AS PP ON PP.ID_PEDIDO=P.ID
INNER JOIN PRODUTOS ON PRODUTOS.ID = PP.ID_PRODUTO
INNER JOIN CLIENTES ON P.ID_CLIENTE = CLIENTES.ID 