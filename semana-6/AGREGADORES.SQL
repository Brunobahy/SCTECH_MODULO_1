USE DB_LOJA
GO


SELECT NOME, PRECO, QUANTIDADE, PRECO*QUANTIDADE AS [TOTAL A GANHAR] FROM PRODUTOS
SELECT SUM(PRECO*QUANTIDADE) AS [TOTAL A GANHAR] FROM PRODUTOS


SELECT * FROM CLIENTES AS C
INNER JOIN PEDIDOS AS P ON P.ID_CLIENTE = C.ID


SELECT C.ID,C.NOME,P.QUANTIDADE_ITENS FROM CLIENTES AS C
INNER JOIN PEDIDOS AS P ON P.ID_CLIENTE = C.ID
GROUP BY C.ID,C.NOME,P.QUANTIDADE_ITENS